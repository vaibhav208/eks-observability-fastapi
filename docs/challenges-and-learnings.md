# 📊 Implementation Challenges and Practical Insights in Building a Cloud-Native Observability Platform on AWS EKS

## 📝 Overview

This section presents a structured analysis of the challenges encountered during the design and implementation of a cloud-native observability platform deployed on **Amazon Elastic Kubernetes Service (EKS)**. 

The system integrates multiple modern DevOps and cloud-native technologies, including:

* **Infrastructure as Code (IaC)** using Terraform
* **Container orchestration** using Kubernetes (EKS)
* **Application deployment** using Helm
* **Continuous Integration and Continuous Delivery (CI/CD)** using GitHub Actions
* **Observability stack** including Prometheus, Grafana, and Alertmanager

The challenges span across different layers of the system, such as infrastructure provisioning, Kubernetes configuration, application deployment, CI/CD automation, and monitoring integration.

Each challenge is documented with:

* A clear description of the issue
* The underlying root cause
* The solution or approach taken
* The related system component

This structured documentation reflects real-world DevOps practices and highlights key learnings in building scalable, automated, and observable cloud-native systems.

---

## 📊 Batch 1: Infrastructure & Terraform Challenges (1–6)

| # | Challenge Title                           | Description                                                                                                   | Root Cause                                                                                                            | Solution / Approach                                                                                                                             | Related Component                      |
| - | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 1 | Cross-module resource referencing failure | Terraform execution failed when different modules tried to directly access each other's resources.            | Terraform modules are isolated by design, but the apps layer tried to directly use infrastructure resources.          | Exported required values (like IAM role ARNs) from the infrastructure layer using outputs and passed them as input variables to the apps layer. | Terraform (Modules & State Management) |
| 2 | EKS access not working properly           | The Kubernetes cluster could not recognize AWS Identity and Access Management (IAM) roles, preventing access. | IAM roles were created, but Kubernetes Role-Based Access Control (RBAC) mapping via `aws-auth` ConfigMap was missing. | Mapped IAM roles to Kubernetes roles (e.g., node roles → `system:nodes`, admin roles → `system:masters`) in `aws-auth`.                         | EKS (Authentication & RBAC)            |
| 3 | Manual input required for IAM role ARNs   | Deployment required manual input for important values like node role ARN, increasing errors and effort.       | Terraform variables were not dynamically connected between infrastructure and application layers.                     | Used Terraform remote state to fetch outputs automatically instead of relying on `.tfvars`.                                                     | Terraform (Remote State & Automation)  |
| 4 | Incorrect usage of `terraform validate`   | Validation command failed when used with variable inputs.                                                     | Misunderstanding of `terraform validate`, which only checks syntax and not runtime values.                            | Used `terraform validate` only for syntax checking and passed variables during `plan` or `apply`.                                               | Terraform (CLI Usage)                  |
| 5 | Helm provider configuration error         | Terraform failed due to incorrect Helm provider configuration.                                                | Provider configuration syntax was outdated or incompatible with the installed version.                                | Updated Helm provider configuration to match the correct version syntax.                                                                        | Terraform (Helm Provider)              |
| 6 | EKS cluster not found (region mismatch)   | Terraform could not locate the EKS cluster even though it existed.                                            | Infrastructure and application layers were configured in different AWS regions.                                       | Standardized AWS region (e.g., `us-east-1`) across all layers.                                                                                  | AWS / Terraform (Region Configuration) |

---
## 📊 Batch 2: Terraform Modules, Helm & Deployment Challenges (7–12)

| #  | Challenge Title                               | Description                                                                                                         | Root Cause                                                                                                          | Solution / Approach                                                                          | Related Component                       |
| -- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------- |
| 7  | Remote state data source failed to load       | The application layer could not retrieve outputs (like cluster details or IAM roles) from the infrastructure layer. | Terraform remote state was misconfigured or incorrectly referenced.                                                 | Fixed `terraform_remote_state` configuration with correct backend settings and output names. | Terraform (Remote State)                |
| 8  | Helm chart path not found                     | Deployment failed because Terraform could not locate the Helm chart files.                                          | Incorrect relative file path was used when referencing Helm charts.                                                 | Corrected the path (e.g., `../../helm/fastapi`) based on execution directory.                | Helm / Terraform (File Paths)           |
| 9  | Helm release failed due to YAML errors        | Helm deployment failed even though configuration looked correct at first glance.                                    | YAML formatting issues such as wrong indentation or missing quotes.                                                 | Carefully validated and corrected YAML syntax before deployment.                             | Helm (YAML Configuration)               |
| 10 | CRDs not installed before dependent resources | Resources like ServiceMonitor and PrometheusRule failed to deploy.                                                  | Custom Resource Definitions (CRDs) from Prometheus Operator were not available before applying dependent resources. | Ensured proper order using `depends_on` and deployed CRDs before dependent resources.        | Kubernetes (CRDs & Observability Stack) |
| 11 | Helm values misconfiguration                  | Services were deployed but behaved incorrectly or unexpectedly.                                                     | Incorrect or incomplete Helm values configuration.                                                                  | Reviewed and aligned values with official Helm chart documentation.                          | Helm (Configuration Management)         |
| 12 | Terraform apply failed due to timing issues   | Deployment failed because some resources were created before the cluster or dependencies were ready.                | Lack of proper sequencing caused race conditions during provisioning.                                               | Added explicit dependencies (`depends_on`) and ensured cluster readiness before deployment.  | Terraform (Dependency Management)       |

---
## 📊 Batch 3: Kubernetes & Observability Integration Challenges (13–18)

| #  | Challenge Title                                           | Description                                                                                                                           | Root Cause                                                                                                | Solution / Approach                                                                                                                              | Related Component                      |
| -- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------- |
| 13 | Incorrect namespace usage for applications and monitoring | The FastAPI application and observability tools were deployed in the wrong or shared namespaces, causing access and discovery issues. | Lack of clear separation between application and monitoring components during modularization.             | Separated namespaces properly: applications in `default/observability` and monitoring tools (Prometheus, Grafana, Alertmanager) in `monitoring`. | Kubernetes (Namespaces & Architecture) |
| 14 | ServiceMonitor failed to detect FastAPI service           | Prometheus was not able to collect metrics from the FastAPI application.                                                              | `namespaceSelector` in ServiceMonitor did not match the namespace where FastAPI was deployed.             | Updated `namespaceSelector.matchNames` to include the correct namespace.                                                                         | Prometheus (ServiceMonitor)            |
| 15 | Label mismatch prevented service discovery                | Prometheus could not discover FastAPI targets even though ServiceMonitor existed.                                                     | Label key mismatch (`App` vs `app`) due to case sensitivity in Kubernetes labels.                         | Corrected label key to exactly match deployment labels (`app: fastapi`).                                                                         | Kubernetes (Labels & Selectors)        |
| 16 | FastAPI missing metrics and health endpoints              | Monitoring and health checks were not working for the application.                                                                    | FastAPI application did not expose `/metrics` and `/health` endpoints initially.                          | Added Prometheus instrumentation and implemented `/metrics` and `/health` endpoints.                                                             | Application (FastAPI Observability)    |
| 17 | Grafana dashboards not loading automatically              | Dashboards were not visible even after deployment of Grafana.                                                                         | Missing configuration for dashboard sidecar and provider in Grafana setup.                                | Enabled dashboard sidecar and configured provider with labeled ConfigMaps.                                                                       | Grafana (Dashboard Provisioning)       |
| 18 | ECR repository existed but image unavailable              | Application deployment failed because no container image was available.                                                               | Only the Amazon Elastic Container Registry (ECR) repository was created; image was never built or pushed. | Built Docker image, tagged it correctly, authenticated with ECR, and pushed the image.                                                           | AWS ECR / CI-CD (Image Management)     |

---
## 📊 Batch 4: Deployment, Image & Access Challenges (19–24)

| #  | Challenge Title                                         | Description                                                                                   | Root Cause                                                                               | Solution / Approach                                                                                   | Related Component                       |
| -- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | --------------------------------------- |
| 19 | Incorrect container image URI in deployment             | Pods failed to start because they tried to pull an invalid or non-existent image.             | Image repository was hardcoded with the wrong AWS account ID.                            | Replaced hardcoded values with dynamic ECR repository URLs using Terraform outputs.                   | Kubernetes / ECR / Terraform            |
| 20 | Pods failing with ImagePullBackOff or 403 errors        | Kubernetes pods could not pull container images, leading to deployment failure.               | Incorrect image URI or missing permissions for nodes to access ECR.                      | Corrected image URI and ensured node IAM role had ECR read permissions.                               | Kubernetes / IAM / ECR                  |
| 21 | Helm release stuck in failed state                      | Deployment could not proceed because Helm considered the release already existing but failed. | Previous failed Helm deployment left stale metadata in the cluster.                      | Cleaned up failed Helm release and redeployed.                                                        | Helm (Release Management)               |
| 22 | FastAPI service inaccessible due to namespace confusion | Service appeared missing or unreachable when trying to access it.                             | Incorrect assumption about the namespace where FastAPI was deployed.                     | Used `kubectl get svc --all-namespaces` to locate the service and accessed it correctly.              | Kubernetes (Namespaces & Debugging)     |
| 23 | FastAPI not accessible from browser                     | Application was running but not reachable externally.                                         | Service type was ClusterIP, which allows only internal communication within the cluster. | Used port-forwarding for testing and identified need for Ingress or LoadBalancer for external access. | Kubernetes (Service Types & Networking) |
| 24 | Grafana port-forwarding failed                          | Unable to access Grafana locally despite it running correctly.                                | Used container port instead of service port in port-forward command.                     | Corrected port-forward mapping (local port → service port).                                           | Kubernetes (Networking & Debugging)     |

---
## 📊 Batch 5: Alerting, Alertmanager & Observability Challenges (25–30)

| #  | Challenge Title                                           | Description                                                                         | Root Cause                                                                                   | Solution / Approach                                                                                         | Related Component                       |
| -- | --------------------------------------------------------- | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| 25 | Alertmanager failed to start due to invalid configuration | Alertmanager service was deployed but failed to initialize properly.                | Configuration referenced a receiver named `null`, but it was not defined correctly.          | Added a receiver explicitly named `null` to match the default configuration expectations.                   | Alertmanager (Configuration)            |
| 26 | Alertmanager resource created but no pods running         | Alertmanager appeared deployed, but no actual pods were running.                    | Invalid configuration prevented the Kubernetes operator from creating the StatefulSet.       | Checked generated configuration and operator logs, fixed errors, and allowed proper pod creation.           | Kubernetes Operator / Alertmanager      |
| 27 | Alerts not sent due to incorrect webhook endpoint         | Alerts were triggered but not delivered to external systems.                        | Used `127.0.0.1` (localhost), which refers to the container itself, not the external system. | Replaced placeholder endpoint with a valid external notification system (e.g., email or Slack).             | Alertmanager (Notification Integration) |
| 28 | Alert pipeline not validated end-to-end                   | Alerts were configured but not tested, leading to uncertainty in reliability.       | No simulation of failure scenarios to verify alert flow.                                     | Simulated failures (e.g., scaling FastAPI to zero) and verified alerts through Prometheus and Alertmanager. | Observability (Testing & Validation)    |
| 29 | Bastion host had excessive permissions                    | Bastion host had full administrative access to the cluster, posing a security risk. | IAM role mapped to `system:masters` and SSH access was open to all.                          | Identified need to restrict access and apply least privilege principles.                                    | AWS IAM / Security                      |
| 30 | Grafana credentials stored in plain text                  | Sensitive login credentials were exposed in configuration files.                    | Username and password were hardcoded in Helm values.                                         | Marked for improvement by moving credentials to secure storage (e.g., Kubernetes Secrets).                  | Grafana / Security                      |

---
## 📊 Batch 6: System Design, Hardening & Workflow Challenges (31–33)

| #  | Challenge Title                                | Description                                                                           | Root Cause                                                                          | Solution / Approach                                                                           | Related Component                |
| -- | ---------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------- |
| 31 | FastAPI deployment not production-ready        | The application was working but lacked features required for production environments. | Missing resource limits (CPU/memory) and no proper external exposure strategy.      | Identified improvements: add resource requests/limits and expose via Ingress or LoadBalancer. | Kubernetes (Workloads & Scaling) |
| 32 | Local development environment lacked tooling   | Development and debugging were harder due to missing tools.                           | Helm Command Line Interface (CLI) was not installed locally.                        | Used kubectl temporarily and planned to install Helm CLI for better workflow.                 | Developer Environment / Tooling  |
| 33 | Terraform deployment required staged execution | Full system deployment could not be done in one step.                                 | Dependencies existed between infrastructure, observability stack, and applications. | Adopted staged deployment approach: infrastructure → observability → applications.            | Terraform (Deployment Strategy)  |

---
## 📊 Batch 7: CI/CD & Developer Workflow Challenges (34–48)

| #  | Challenge Title                                      | Description                                                                      | Root Cause                                                                | Solution / Approach                                                       | Related Component                 |
| -- | ---------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------- | --------------------------------- |
| 34 | Git repository misconfiguration                      | Code repository was not properly connected, causing issues with version control. | Existing Git remote conflicted with new setup.                            | Updated remote using `git remote set-url`.                                | Git / Version Control             |
| 35 | Docker build uncertainty                             | Uncertainty whether the Docker image would work in CI/CD.                        | Dockerfile was not tested before pipeline integration.                    | Built and tested Docker image locally before using in CI/CD.              | Docker / CI-CD                    |
| 36 | ECR push failed due to authentication                | Unable to push Docker image to Amazon Elastic Container Registry (ECR).          | Missing authentication with AWS before pushing image.                     | Used AWS CLI login (`aws ecr get-login-password`) to authenticate Docker. | AWS ECR / CI-CD                   |
| 37 | GitHub Actions workflow setup confusion              | Initial pipeline setup was unclear and incomplete.                               | No existing workflow file or structure.                                   | Created a basic workflow and improved it incrementally.                   | GitHub Actions (CI/CD)            |
| 38 | Static credentials vs OIDC confusion                 | Difficulty choosing between ease and security for authentication.                | Lack of clarity between static AWS credentials and OpenID Connect (OIDC). | Implemented OIDC with IAM roles for secure authentication.                | AWS IAM / CI-CD Security          |
| 39 | Terraform modularization break                       | Terraform structure became inconsistent and hard to manage.                      | IAM resources were placed in root instead of modules.                     | Refactored and moved IAM resources into a dedicated module.               | Terraform (Modular Design)        |
| 40 | Bash variable expansion errors                       | Scripts failed unexpectedly during execution.                                    | Shell interpreted `${}` syntax incorrectly during execution.              | Used quoted heredoc (`<<'EOF'`) to prevent shell interpolation.           | Shell Scripting / Automation      |
| 41 | Terraform module isolation issue                     | Modules failed when trying to access resources directly.                         | Attempted direct access to root-level resources from modules.             | Passed required values as input variables between modules.                | Terraform (Module Design)         |
| 42 | EKS access failure in CI/CD                          | CI/CD pipeline could not access the Kubernetes cluster.                          | Missing IAM permissions for AWS CLI operations.                           | Added required permission (`eks:DescribeCluster`) to IAM role.            | AWS IAM / CI-CD                   |
| 43 | kubeconfig setup failure in CI                       | CI pipeline could not connect to the cluster.                                    | IAM role not mapped in Kubernetes Role-Based Access Control (RBAC).       | Updated `aws-auth` ConfigMap to map IAM role correctly.                   | EKS (Authentication & RBAC)       |
| 44 | Helm deployment failure in CI/CD                     | Deployment step failed during pipeline execution.                                | Incorrect Helm chart path specified in workflow.                          | Corrected path to `./helm/fastapi`.                                       | Helm / CI-CD                      |
| 45 | Difficulty debugging CI/CD failures                  | Root causes of failures were unclear during pipeline runs.                       | Logs were not fully reviewed.                                             | Analyzed complete logs to identify exact issues.                          | CI/CD Debugging                   |
| 46 | Manual aws-auth configuration risk                   | Manual changes were lost after infrastructure recreation.                        | `aws-auth` ConfigMap was edited manually instead of managed by code.      | Moved configuration into Terraform for automation.                        | Terraform / Kubernetes            |
| 47 | Difficult re-deployment after infrastructure changes | Re-deployment required manual effort after infrastructure reset.                 | No automated redeployment trigger existed.                                | Created scripts (e.g., `deploy.sh`) to simplify redeployment.             | CI/CD / Automation                |
| 48 | Manual kubeconfig setup overhead                     | Repeated manual steps were needed to access the cluster.                         | Local environment dependency and lack of automation.                      | Created script (`connect.sh`) to automate cluster access.                 | Developer Experience / Automation |

---
## 📊 Batch 8: Architecture, Helm, Ingress & CI/CD Design Challenges (49–60)

| #  | Challenge Title                                 | Description                                                                | Root Cause                                                                | Solution / Approach                                                  | Related Component                  |
| -- | ----------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------- | ---------------------------------- |
| 49 | Supporting multi-service architecture           | System initially assumed only one application, causing design limitations. | Architecture was not designed for multiple services (backend + frontend). | Created separate Helm charts for FastAPI and Website services.       | System Design / Helm               |
| 50 | CI/CD pipeline limited to one application       | Pipeline could only handle a single Docker image.                          | Initial pipeline design did not consider multiple services.               | Extended GitHub Actions workflow to build and push multiple images.  | CI/CD (GitHub Actions)             |
| 51 | Overengineering frontend choice                 | Considered complex frameworks unnecessarily.                               | Lack of clear requirements led to overcomplication (e.g., Next.js).       | Selected a simple static website served via NGINX.                   | Application Design                 |
| 52 | Helm chart path errors in CI/CD                 | Deployment failed due to missing chart files.                              | Incorrect Helm chart path specified in pipeline.                          | Corrected path to match repository structure (`./helm/fastapi`).     | Helm / CI-CD                       |
| 53 | Tight coupling between Terraform and Helm       | Infrastructure and application deployments were mixed together.            | Terraform was used to manage both infra and application releases.         | Decoupled Helm deployments from Terraform.                           | Terraform / Helm Architecture      |
| 54 | Difficulty managing multiple Helm deployments   | Deploying multiple services became complex and unorganized.                | Helm charts were not modular or reusable.                                 | Created separate, reusable Helm charts for each service.             | Helm (Modularization)              |
| 55 | Docker image push workflow confusion            | Difficulty understanding how to integrate Docker with ECR.                 | Lack of prior experience with ECR workflow.                               | Implemented manual authentication first, then automated via CI/CD.   | Docker / AWS ECR                   |
| 56 | Security confusion: secrets vs OIDC             | Uncertainty about secure authentication approach.                          | Trade-off between simplicity (static secrets) and security (OIDC).        | Implemented OpenID Connect (OIDC) with IAM roles.                    | AWS IAM / CI-CD Security           |
| 57 | IAM role not recognized inside cluster          | CI/CD roles could not interact with Kubernetes.                            | Missing mapping in `aws-auth` ConfigMap.                                  | Added IAM role mapping to Kubernetes RBAC.                           | EKS (Authentication & RBAC)        |
| 58 | Terraform module could not access OIDC provider | Module failed due to missing dependency.                                   | OIDC provider defined only in root module.                                | Passed OIDC ARN as input variable to module.                         | Terraform (Module Inputs)          |
| 59 | Terraform provider warnings                     | Terraform produced warnings during execution.                              | Missing `required_providers` definition in modules.                       | Added provider definitions in `versions.tf`.                         | Terraform (Provider Configuration) |
| 60 | Manual Ingress controller installation          | Ingress was installed manually, breaking automation.                       | Used manual `kubectl apply` instead of Infrastructure as Code (IaC).      | Migrated Ingress controller installation to Terraform Helm provider. | Kubernetes / Terraform (Ingress)   |

---
## 📊 Batch 9: Ingress, Routing, Terraform State & System Stability Challenges (61–72)

| #  | Challenge Title                               | Description                                                        | Root Cause                                                                 | Solution / Approach                                                    | Related Component                  |
| -- | --------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------- |
| 61 | Ingress lost after Terraform re-deployment    | Ingress controller disappeared after infrastructure changes.       | It was not managed by Terraform, so it was not recreated automatically.    | Added Ingress controller as a Helm release in Terraform.               | Terraform / Kubernetes (Ingress)   |
| 62 | 503 errors when accessing observability tools | Services like Grafana and Prometheus were unreachable via browser. | Namespace mismatch between Ingress and backend services.                   | Used ExternalName services to route traffic across namespaces.         | Kubernetes (Ingress & Services)    |
| 63 | Ingress not receiving external IP             | External access was not available for services.                    | Multiple conflicting ingress resources created confusion for LoadBalancer. | Consolidated into a single ingress definition.                         | Kubernetes (Ingress Controller)    |
| 64 | Duplicate route/path errors in NGINX          | Deployment failed due to duplicate paths.                          | Same route defined multiple times in different ingress resources.          | Removed duplicate routes and ensured unique path definitions.          | Kubernetes (Ingress Rules)         |
| 65 | Grafana redirect loop issue                   | Grafana UI kept redirecting and was not accessible.                | Application was not configured for subpath routing (`/grafana`).           | Configured `root_url` and enabled `serve_from_sub_path` in Grafana.    | Grafana / Ingress                  |
| 66 | Prometheus returning 404 errors               | Prometheus UI was not accessible via Ingress.                      | Incorrect routing configuration for subpath access.                        | Set `externalUrl` and `routePrefix` correctly in configuration.        | Prometheus / Ingress               |
| 67 | Observability broken due to rewrite rules     | Some services stopped working after applying global rewrite rules. | Same rewrite rule applied to all services regardless of requirements.      | Split ingress rules and applied rewrite only where needed (e.g., API). | Kubernetes (Ingress Configuration) |
| 68 | Ingress could not route across namespaces     | Services in different namespaces were unreachable through Ingress. | Kubernetes Ingress is namespace-scoped.                                    | Created proxy services (ExternalName) to bridge namespaces.            | Kubernetes (Networking)            |
| 69 | Observability ingress not exposed properly    | Monitoring tools were not accessible externally.                   | Multiple ingress definitions across namespaces caused confusion.           | Unified ingress into a single entry point.                             | Kubernetes (Ingress Architecture)  |
| 70 | Terraform “resource already exists” errors    | Terraform failed when trying to create existing resources.         | Resources were created manually or outside Terraform.                      | Imported existing resources using `terraform import`.                  | Terraform (State Management)       |
| 71 | Terraform import failed due to partial state  | Importing resources caused errors.                                 | Resource was partially tracked in Terraform state.                         | Cleaned state using `terraform state rm` before re-importing.          | Terraform (State Handling)         |
| 72 | Duplicate resource definitions in Terraform   | Deployment failed due to conflicting definitions.                  | Same resource defined multiple times in code.                              | Removed duplicate definitions to ensure one resource per block.        | Terraform (Code Consistency)       |

---
## 📊 Batch 10: Infrastructure Lifecycle, CI/CD Stability & Application Design Challenges (73–86)

| #  | Challenge Title                                     | Description                                        | Root Cause                                                                                            | Solution / Approach                                                           | Related Component                 |
| -- | --------------------------------------------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | --------------------------------- |
| 73 | System instability due to mixed management          | System behaved inconsistently after changes.       | Combination of manual changes and Terraform-managed resources.                                        | Migrated all resources to Terraform for consistency.                          | Terraform / System Architecture   |
| 74 | Non-reproducible deployments                        | Deployments produced different results each time.  | Manual steps introduced inconsistencies.                                                              | Fully automated deployments using Terraform and CI/CD.                        | CI/CD / Terraform                 |
| 75 | Terraform destroy failed (subnets in use)           | Infrastructure cleanup failed during teardown.     | AWS resources like Elastic Load Balancer (ELB) and network interfaces were still attached to subnets. | Deleted Kubernetes Ingress and LoadBalancer resources before running destroy. | AWS Networking / Terraform        |
| 76 | Internet Gateway could not detach                   | VPC deletion failed due to dependency issues.      | Active public IP mappings and load balancer dependencies.                                             | Removed dependent networking resources before detaching Internet Gateway.     | AWS Networking                    |
| 77 | ECR repository deletion failed                      | Terraform could not delete container registry.     | Repository still contained images.                                                                    | Deleted images manually or enabled `force_delete`.                            | AWS ECR                           |
| 78 | Terraform destroy failed due to dependency order    | Destroy process failed midway.                     | Apps layer depended on infrastructure outputs that were already removed.                              | Destroyed apps layer first, then infrastructure layer.                        | Terraform (Dependency Order)      |
| 79 | CRD resources failed during destroy                 | Observability resources could not be removed.      | Custom Resource Definitions (CRDs) were deleted before dependent resources.                           | Deleted dependent resources before removing CRDs.                             | Kubernetes (CRDs Lifecycle)       |
| 80 | Namespace stuck in terminating state                | Kubernetes namespace could not be deleted.         | Resources with finalizers still existed inside namespace.                                             | Removed remaining resources and cleared finalizers manually.                  | Kubernetes (Namespace Management) |
| 81 | Duplicate ingress after infrastructure recreation   | Old routes caused conflicts after redeployment.    | Stale ingress resources remained in cluster.                                                          | Deleted old ingress resources before redeploying.                             | Kubernetes (Ingress)              |
| 82 | Resource conflicts due to inconsistent namespaces   | Services conflicted after redeployment.            | Different components deployed in inconsistent namespaces.                                             | Standardized namespace usage across system.                                   | Kubernetes (Namespaces)           |
| 83 | Duplicate API routes after redeployment             | Same API path caused routing conflicts.            | Multiple ingress resources defined same route.                                                        | Ensured single ownership for each route.                                      | Kubernetes (Routing)              |
| 84 | CI/CD failed due to outdated paths                  | Pipeline broke after project restructuring.        | Repository structure changed but pipeline paths were not updated.                                     | Updated GitHub Actions paths to match new structure.                          | CI/CD (GitHub Actions)            |
| 85 | Non-deterministic deployments using latest tag      | Deployments were inconsistent across environments. | Used mutable image tag (`latest`).                                                                    | Switched to immutable tagging using commit SHA.                               | Docker / CI-CD                    |
| 86 | Deployment failed due to existing ingress conflicts | CI/CD deployments failed unexpectedly.             | Old ingress resources already owned the same routes.                                                  | Removed stale resources and enforced single route ownership.                  | Kubernetes / CI-CD                |

---
## 📊 Batch 11: Observability, Cross-Cutting & Alerting Challenges (87–100)

| #   | Challenge Title                                   | Description                                                                       | Root Cause                                                             | Solution / Approach                                             | Related Component                 |
| --- | ------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------- |
| 87  | Inconsistent application repository structure     | Application code was scattered across multiple folders, making deployment harder. | No standardized structure for multiple services.                       | Reorganized into `apps/fastapi` and `apps/website`.             | Application Architecture          |
| 88  | Overengineered frontend design                    | Considered complex frontend frameworks unnecessarily.                             | Lack of clear requirements led to overdesign.                          | Chose simple static website served via NGINX.                   | Application Design                |
| 89  | Static ingress naming caused conflicts            | Ingress resources conflicted during redeployment.                                 | Fixed names reused across deployments.                                 | Made ingress names dynamic using Helm release names.            | Kubernetes / Helm                 |
| 90  | Namespace mismatch between workloads and ingress  | Services were unreachable due to incorrect routing.                               | Workloads and ingress deployed in different namespaces inconsistently. | Standardized namespace usage per workload.                      | Kubernetes (Namespaces & Routing) |
| 91  | Alertmanager service existed but no pods running  | Alertmanager UI was unavailable even though service existed.                      | Configuration errors prevented pod creation.                           | Fixed configuration and allowed operator to create StatefulSet. | Alertmanager / Kubernetes         |
| 92  | Missing null receiver in Alertmanager             | Alertmanager failed due to invalid configuration.                                 | Receiver `null` referenced but not defined.                            | Added explicit `null` receiver in configuration.                | Alertmanager                      |
| 93  | Alertmanager UI returned 404 via Ingress          | Could not access Alertmanager through browser.                                    | Incorrect path routing without proper rewrite rules.                   | Applied correct rewrite rules for Alertmanager path.            | Kubernetes Ingress / Alertmanager |
| 94  | Global rewrite rule broke observability tools     | Grafana and Prometheus stopped working after routing changes.                     | Same rewrite rule applied to all services.                             | Split ingress rules based on service requirements.              | Kubernetes (Ingress Rules)        |
| 95  | Incorrect regex path handling in Ingress          | Routing failed when using regex paths.                                            | Used regex with incorrect `pathType`.                                  | Updated `pathType` to `ImplementationSpecific`.                 | Kubernetes Ingress                |
| 96  | Duplicate ingress paths rejected                  | Deployment failed due to route conflicts.                                         | Same path defined in multiple ingress resources.                       | Removed duplicates and enforced single ownership.               | Kubernetes Ingress                |
| 97  | Cross-namespace routing issues                    | Observability services were not reachable.                                        | Ingress and services were in different namespaces.                     | Used proxy services or unified ingress design.                  | Kubernetes Networking             |
| 98  | Alerts visible in Prometheus but not Alertmanager | Alerts were triggered but not forwarded.                                          | Alertmanager configuration was incorrect or unhealthy.                 | Fixed configuration and verified alert pipeline.                | Prometheus / Alertmanager         |
| 99  | CRD lifecycle issues in observability stack       | Deployment and deletion of monitoring resources failed.                           | CRDs were removed before dependent resources.                          | Managed CRD lifecycle carefully with correct sequencing.        | Kubernetes (CRDs)                 |
| 100 | Terraform module dependency confusion             | Modules failed due to improper referencing.                                       | Resources referenced without proper inputs/outputs.                    | Used explicit variables and outputs for communication.          | Terraform (Modules)               |

---
## 📊 Batch 12: Final Alerting, Debugging & System Reliability Challenges (101–112)

| #   | Challenge Title                                     | Description                                              | Root Cause                                                                | Solution / Approach                                                       | Related Component                     |
| --- | --------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ------------------------------------- |
| 101 | Bash scripting errors in automation                 | Automation scripts failed unexpectedly during execution. | Shell variables were incorrectly expanded, causing syntax errors.         | Used quoted heredoc (`<<'EOF'`) to prevent unwanted variable expansion.   | Shell Scripting / Automation          |
| 102 | State drift between Terraform, Helm, and kubectl    | System became inconsistent and difficult to manage.      | Multiple tools were managing the same resources independently.            | Defined a single source of truth per resource (primarily Terraform).      | Terraform / Kubernetes                |
| 103 | Excessive manual operational steps                  | Setup and deployment required repetitive manual work.    | Lack of automation for common tasks.                                      | Created scripts (e.g., bootstrap, cleanup) to automate workflows.         | DevOps Workflow / Automation          |
| 104 | EKS access issues due to missing aws-auth mapping   | Users and CI/CD could not access the cluster.            | IAM roles were not mapped in Kubernetes Role-Based Access Control (RBAC). | Added role mappings in `aws-auth` ConfigMap.                              | EKS (Authentication & RBAC)           |
| 105 | OIDC and IAM integration complexity                 | Difficulty setting up secure authentication for CI/CD.   | Misconfiguration in trust relationship between GitHub and AWS.            | Properly configured OpenID Connect (OIDC) provider and IAM roles.         | AWS IAM / CI-CD Security              |
| 106 | Hardcoded secrets in configuration                  | Sensitive credentials were exposed in code.              | Secrets (e.g., SMTP credentials) stored in plain text.                    | Planned migration to secure secret management (e.g., Kubernetes Secrets). | Security / Configuration Management   |
| 107 | Ingress ownership conflicts                         | Routing conflicts occurred across deployments.           | Static naming and duplicate route ownership.                              | Used dynamic naming and enforced single route ownership.                  | Kubernetes Ingress                    |
| 108 | AWS resource cleanup delays                         | Infrastructure deletion did not happen immediately.      | Cloud provider uses asynchronous cleanup and dependency resolution.       | Waited and removed resources in correct dependency order.                 | AWS / Terraform                       |
| 109 | Alerts firing but not visible in Alertmanager       | Alerts were generated but not received by Alertmanager.  | Incorrect service port mapping between Prometheus and Alertmanager.       | Fixed service port configuration (`http-web`).                            | Prometheus / Alertmanager             |
| 110 | Prometheus not sending alerts to Alertmanager       | Alert pipeline was broken.                               | Alertmanager endpoint not discovered at runtime.                          | Verified runtime metrics and corrected endpoint configuration.            | Prometheus                            |
| 111 | Silent alert delivery failure                       | Alerts failed without visible errors.                    | No validation of alert delivery pipeline.                                 | Used Prometheus internal metrics to verify delivery status.               | Observability (Monitoring Validation) |
| 112 | Alerts visible but email notifications not received | Notifications were not reaching users.                   | Invalid or placeholder email credentials used.                            | Configured valid Gmail SMTP settings with proper authentication.          | Alertmanager / Email Integration      |

---
